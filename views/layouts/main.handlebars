<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Job Finder</title>
    <!-- Tailwind CSS (gerado) -->
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-gray-900 text-gray-100 min-h-screen flex flex-col">
    <header class="bg-gray-900 border-b border-gray-800 p-4 flex justify-between items-center">
      <a class="text-xl font-bold" href="/">Job Finder</a>
      <nav>
        <ul class="flex space-x-4">
          <li>
            <a class="hover:text-blue-400 transition" href="/">Ver Vagas</a>
          </li>
          <li>
            <a 
              class="bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded transition" 
              href="/jobs/add">
              Abrir Vaga
            </a>
          </li>
        </ul>
      </nav>
    </header>

    <main class="flex-1">
      {{{ body }}}
    </main>

    <footer class="bg-gray-900 text-gray-500 text-sm p-4 text-center border-t border-gray-800">
      © 2025 Job Finder - Todos os direitos reservados
    </footer>
  </body>
</html>